# Python
backend/.venv/
__pycache__/
*.pyc

# Node
frontend/node_modules/
frontend/dist/

# All local data (decks, annotations, exports) lives in Google Drive, not git.
/data/

# Local render cache (PNGs regenerated from PDFs) + per-machine config
.cache/
.env

# Captured gamma.app login session for the PPTX importer — SENSITIVE (live session
# cookies). Default lives under .cache/ already; this guards custom paths too.
*gamma_auth_state*.json

.DS_Store
